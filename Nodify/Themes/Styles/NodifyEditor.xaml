<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Nodify">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    <local:UnscaleTransformConverter x:Key="UnscaleTransformConverter" />
    <local:ScaleDoubleConverter x:Key="ScaleDoubleConverter" />
    <local:ScalePointConverter x:Key="ScalePointConverter" />

    <DataTemplate x:Key="ConnectionTemplate">
        <TextBlock Text="ConnectionTemplate" />
    </DataTemplate>

    <DataTemplate x:Key="PendingConnectionTemplate">
        <TextBlock Text="PendingConnectionTemplate" />
    </DataTemplate>

    <Style x:Key="SelectionRectangleStyle" TargetType="Rectangle">
        <Setter Property="Stroke" Value="DodgerBlue" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Fill">
            <Setter.Value>
                <SolidColorBrush Opacity="0.1" Color="DodgerBlue" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:NodifyEditor}">
        <Setter Property="ClipToBounds" Value="True" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="#1E1E1E" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="EnableRealtimeSelection" Value="True" />
        <Setter Property="DisplayConnectionsOnTop" Value="False" />
        <Setter Property="SelectionRectangleStyle" Value="{StaticResource SelectionRectangleStyle}" />
        <Setter Property="ConnectionTemplate" Value="{StaticResource ConnectionTemplate}" />
        <Setter Property="PendingConnectionTemplate" Value="{StaticResource PendingConnectionTemplate}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NodifyEditor}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Canvas RenderTransform="{TemplateBinding ViewportTransform}">
                            <local:NodifyCanvas
                                x:Name="PART_ItemsHost"
                                Extent="{Binding ItemsExtent, Mode=OneWayToSource, RelativeSource={RelativeSource TemplatedParent}}"
                                IsItemsHost="True" />

                            <TextBlock x:Name="PART_ConnectionsHost" Text="ConnectionsMultiSelector" />

                            <ContentPresenter ContentSource="PendingConnection" />

                            <Rectangle
                                Canvas.Left="{Binding SelectedArea.X, RelativeSource={RelativeSource TemplatedParent}}"
                                Canvas.Top="{Binding SelectedArea.Y, RelativeSource={RelativeSource TemplatedParent}}"
                                RenderTransform="{Binding ViewportTransform, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource UnscaleTransformConverter}}"
                                Style="{TemplateBinding SelectionRectangleStyle}"
                                Visibility="{TemplateBinding IsSelecting,
                                                             Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Rectangle.Width>
                                    <MultiBinding Converter="{StaticResource ScaleDoubleConverter}">
                                        <Binding Path="SelectedArea.Width" RelativeSource="{RelativeSource TemplatedParent}" />
                                        <Binding Path="ViewportZoom" RelativeSource="{RelativeSource TemplatedParent}" />
                                    </MultiBinding>
                                </Rectangle.Width>
                                <Rectangle.Height>
                                    <MultiBinding Converter="{StaticResource ScaleDoubleConverter}">
                                        <Binding Path="SelectedArea.Height" RelativeSource="{RelativeSource TemplatedParent}" />
                                        <Binding Path="ViewportZoom" RelativeSource="{RelativeSource TemplatedParent}" />
                                    </MultiBinding>
                                </Rectangle.Height>
                            </Rectangle>

                            <local:DecoratorsControl
                                IsTabStop="False"
                                ItemContainerStyle="{TemplateBinding DecoratorContainerStyle}"
                                ItemTemplate="{TemplateBinding DecoratorTemplate}"
                                ItemsSource="{TemplateBinding Decorators}">
                                <local:DecoratorsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <local:NodifyCanvas Extent="{Binding DecoratorsExtent, Mode=OneWayToSource, RelativeSource={RelativeSource AncestorType=local:NodifyEditor}}" />
                                    </ItemsPanelTemplate>
                                </local:DecoratorsControl.ItemsPanel>
                            </local:DecoratorsControl>
                        </Canvas>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>